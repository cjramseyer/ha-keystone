---
name: Sync labels

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "34 5 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  workflows:
    uses: >-
      hassio-addons/workflows/.github/workflows/labels.yaml@bf6ee19d0cb5ecaf6d1205d83a48c32ffe6145a7
