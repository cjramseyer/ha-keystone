---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  workflows:
    uses: >-
      hassio-addons/workflows/.github/workflows/lock.yaml@bf6ee19d0cb5ecaf6d1205d83a48c32ffe6145a7
